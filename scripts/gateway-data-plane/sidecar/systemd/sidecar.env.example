# /etc/openclaw/sidecar.env — referenced by openclaw-sidecar.service.
# Owner: root, mode 0640, group = the unix user that runs the sidecar.
# NEVER commit the real file — only this example.

ELEVENLABS_API_KEY=sk_<your-elevenlabs-api-key>
